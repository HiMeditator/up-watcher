[project]
name = "up-watcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lark-oapi>=1.6.4",
    "platformdirs>=4.9.6",
    "requests>=2.34.0",
]

[project.scripts]
upw = "up_watcher.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/up_watcher"]
